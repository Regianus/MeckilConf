//                                    <---(=====)---> MeckilConf <---(=====)--->

cl_allowupload 1                                                                               // Security against malicious servers
alias cl_allowupload "echo [MeckilConf] BLOCKED cl_allowupload!"                               // Security against malicious servers

cl_cmdrate 66; cl_updaterate 66; cl_interp_ratio 1; cl_interp 0.0151515; rate 66000            // IDEAL INTERNET CONNECTION CONFIGURATION.

sv_allow_point_servercommand always                                                            // Security against malicious servers
//alias sv_allow_point_servercommand "echo [MeckilConf] BLOCKED sv_allow_point_servercommand!" // Security against malicious servers

alias unbindall "echo [MeckilConf] BLOCKED unbindall!"	                                       // Security against malicious servers

cl_disablehtmlmotd 1                                                                           // Disables ads when entering community servers. (Very useful! Recommended to keep this enabled.)

alias sndplaydelay "echo [MeckilConf] BLOCKED sndplaydelay!"                                   // Security against malicious servers
alias soundfade "echo [MeckilConf] BLOCKED soundfade!"                                         // Security against malicious servers

alias cl_spec_mode                                                                             // Security against malicious servers
alias rpt_connect                                                                              // Security against malicious servers
alias r_screenoverlay                                                                          // Security against malicious servers
alias closed_htmlpage "echo [MeckilConf] BLOCKED pinion!"                                      // Security against malicious servers
alias cl_interp "echo [MeckilConf] Don't be a spy main."

//-------------------//
